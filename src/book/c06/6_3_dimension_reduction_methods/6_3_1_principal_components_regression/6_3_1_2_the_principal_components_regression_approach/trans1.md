---
layout: default
title: "trans1"
---

# 6.3.1.2 주성분 회귀 접근법 (The Principal Components Regression Approach)

The _principal components regression_ (PCR) approach involves constructing the first $M$ principal components, $Z_1, \ldots, Z_M$, and then using these components as the predictors in a linear regression model that is fit using least squares. The key idea is that often a small number of principal components suffice to explain most of the variability in the data, as well as the relationship with the response. In other words, we assume that _the directions in which $X_1, \ldots, X_p$ show the most variation are the directions that are associated with $Y$_. While this assumption is not guaranteed to be true, it often turns out to be a reasonable enough approximation to give good results. 
_주성분 회귀(principal components regression, PCR)_ 접근법은 기저 데이터의 처음 $M$개 주성분 $Z_1, \ldots, Z_M$을 수립 구축하는 과정을 거친 뒤, 도출된 이 성분들을 최소 제곱법(least squares)으로 적합된 선형 회귀 모델의 예측 변수로 활용하는 체계를 포함합니다. 주요 핵심 아이디어는 소수의 주성분만으로도 데이터 내의 분산 변동성 대부분은 물론, 반응 변수와의 관계성까지 충분히 다 설명해 낼 수 있다는 점입니다. 다시 말해서, 우리는 _$X_1, \ldots, X_p$ 인자들이 가장 막대한 변동성을 보여주는 벡터 방향이 곧 반응 변수 $Y$와 연관된 방향일 것이다_ 라고 가정합니다. 비록 이 가정이 항상 절대적 참이 아닐 수도 있으나, 훌륭한 산출 결과를 타전하기에 꽤나 합리적이고 타당한 근사치임이 종종 입증됩니다.

![Figure 6.17](./img/6_17.png)

**FIGURE 6.17.** _Plots of the second principal component scores $z_{i2}$ versus_ `pop` _and_ `ad` _. The relationships are weak._ 
**그림 6.17.** _두 번째 주성분 구조 점수 $z_{i2}$ 축과 기반 변수_ `pop` _및_ `ad` _간의 대립 상관(versus) 도면 배열 플롯입니다. 둘 간의 연관 관계는 매우 약하고 희미합니다._ 

![Figure 6.18](./img/6_18.png)

**FIGURE 6.18.** _PCR was applied to two simulated data sets. In each panel, the horizontal dashed line represents the irreducible error._ Left: _Simulated data from Figure 6.8._ Right: _Simulated data from Figure 6.9._ 
**그림 6.18.** _PCR 기법이 가상 시뮬레이션된 두 데이터 세트에 일괄 적용되었습니다. 각 패널 도면 면에서 무조건 나타난 가로 수평 점선은 결코 축소 불가한 내재 잔차 오류(irreducible error) 오차 한계망을 대변합니다._ 왼쪽: _그림 6.8 모델에서 끌어온 모의 추출 시뮬레이션 데이터._ 오른쪽: _그림 6.9 출처에서 파생된 모의 시뮬레이션 환경 데이터._ 

If the assumption underlying PCR holds, then fitting a least squares model to $Z_1, \ldots, Z_M$ will lead to better results than fitting a least squares model to $X_1, \ldots, X_p$, since most or all of the information in the data that relates to the response is contained in $Z_1, \ldots, Z_M$, and by estimating only $M \ll p$ coefficients we can mitigate overfitting. In the advertising data, the first principal component explains most of the variance in both `pop` and `ad`, so a principal component regression that uses this single variable to predict some response of interest, such as `sales`, will likely perform quite well. 
만약 앞서 언급한 PCR 밑바탕의 내재 가정이 참으로 성립 유지된다면, $Z_1, \ldots, Z_M$ 단위들에 최소 제곱법 기반 모델을 적합시키는 것은 원래의 $X_1, \ldots, X_p$ 수량에 모델을 적합시키는 것보다 더 명백히 나은 결과를 도출할 것입니다. 왜냐하면 반응 변수와 연계된 데이터의 파장 정보 대부분 혹은 그 전체 일체가 이 $Z_1, \ldots, Z_M$ 궤도 내에 농축되어 포섭되어 있고, 더불어 원시 변수보다 훨씬 적은 수치인 오직 $M \ll p$ 개의 계수만을 추정함으로써 모델의 잠재적 과적합(overfitting) 부작용을 유의미하게 경감시킬 수 있기 때문입니다. 앞선 광고 데이터 분석 예시에서, 제 1 주성분 하나가 투입 지표 `pop` 및 소진 재원 `ad` 요인 양 단면의 압도적 분산 대다수를 대변 설명하므로, 이 단일 축 변수 하나만을 덩그러니 사용해 타겟 `sales` 와 같은 임의 관측 관심사의 반응값을 도출 예측하는 단일 주성분 회귀 모델은 아주 탁월하게 구동 성능을 뽐낼 가능성이 매우 높습니다.

Figure 6.18 displays the PCR fits on the simulated data sets from Figures 6.8 and 6.9. Recall that both data sets were generated using $n = 50$ observations and $p = 45$ predictors. However, while the response in the first data set was a function of all the predictors, the response in the second data set was generated using only two of the predictors. The curves are plotted as a function of $M$, the number of principal components used as predictors in the regression model. As more principal components are used 
부속된 그림 6.18 패널은 기존의 그림 6.8 및 6.9 단락으로부터 추출된 시뮬레이션 모의 데이터 세트 환경에서 파생된 다채로운 PCR 결과 적합 양상을 보여줍니다. 두 데이터 모집단 모두 애초 관측치 $n = 50$ 개와 투입 예측 변수 $p = 45$ 개의 규격으로 조율 파생 생성되었음을 다시 상기해 보십시오. 그러나 첫 번째 세트 군단 내의 목표 반응 변수는 내재된 투입 변수 전원 모두의 다중 조합 함수로 도래된 반면, 이어지는 두 번째 세트 진영 모델 내 반응 변수는 단 단 두 개의 한정된 예측 변수들만을 사용하여 수립 파생된 산출물입니다. 화면의 곡선 궤적들은 회귀 분석의 예측 변수군으로써 투입 운용한 차원 주성분 개수 인자인 총 투입 $M$ 개수의 다항 함수비로서 표시되었습니다. 여기서 회귀 적합 모델 단위 내에 더 무수히 많은 추출 주성분 다수가 순차 사용될수록, 

![Figure 6.19](./img/6_19.png)

**FIGURE 6.19.** _PCR, ridge regression, and the lasso were applied to a simulated data set in which the first five principal components of X contain all the information about the response Y. In each panel, the irreducible error $\text{Var}(\epsilon)$ is shown as a horizontal dashed line._ Left: _Results for PCR._ Right: _Results for lasso (solid) and ridge regression (dotted). The x-axis displays the shrinkage factor of the coefficient estimates, defined as the $\ell_2$ norm of the shrunken coefficient estimates divided by the $\ell_2$ norm of the least squares estimate._ 
**그림 6.19.** _고체 데이터군 $X$ 변수의 초기 수위 5개 분량의 주성분 궤도들이 목적 반응성 $Y$ 에 관한 통계 전반 일체의 정보 무리를 모두 갈무리 흡수 포획한 상황을 상정한 모의 시뮬레이션 데이터 세트 환경에서, PCR 수법 및 능선 회귀(ridge regression), 그리고 연속된 라쏘(lasso) 모델이 동시다발 일괄 투입 적용되었습니다. 각 분리 패널 평면에서 축소 변형 불가능한 부동의 산포 오차 한계 $\text{Var}(\epsilon)$ 현상 규모는 평행 가로줄 가로 점선 형태로 지표 지시됩니다._ 왼쪽 패널: _PCR 기법 운영으로 도달 성립된 산출 결과망._ 오른쪽 패널: _라쏘 모델(단색 실선) 및 위상 능선 회귀 수단(점선 궤적) 의 처리 구동 결과 모음집. 이때 단면 x-축선은 축소 편향 모델 계수 산출 추정치 군단 규모의 축소 인자 범위를 보여주며, 이는 최소 제곱 도달 추정 결산치들의 도합 $\ell_2$ 노름(norm) 대비 현재 축소 압축된 각 계수 추정치의 $\ell_2$ 노름 위상치 지수 비례값으로 산정 정의되었습니다._ 

in the regression model, the bias decreases, but the variance increases. This results in a typical U-shape for the mean squared error. When $M = p = 45$, then PCR amounts simply to a least squares fit using all of the original predictors. The figure indicates that performing PCR with an appropriate choice of $M$ can result in a substantial improvement over least squares, especially in the left-hand panel. However, by examining the ridge regression and lasso results in Figures 6.5, 6.8, and 6.9, we see that PCR does not perform as well as the two shrinkage methods in this example. 
모델의 고유 편향성 오차(bias) 는 감소 기류를 타지만, 반대로 통계 오차 분산(variance) 비중은 증가세로 치솟습니다. 이러한 상충 현상은 결국 평균 제곱 적합 오차(MSE) 수치가 그리는 모양새에서 전형적인 U자형 궤도 곡선을 유도하게 됩니다. 만일 사용 비율을 $M = p = 45$ 인계치로 꽉 채운다면, 그때의 PCR 과정은 본래의 오리지널 예측 변수 전체 일체 무리를 남김없이 다 사용한 최소 제곱 적합 체계와 고작 본질상 온전히 다를 바 없는 결과로 수렴됩니다. 해당 도면은 단면상 왼쪽 좌측 패널 전개 양상에서 볼 수 있듯이, 매우 적절히 조율된 이상적 차원 $M$ 을 심도 있게 선택 채택하여 PCR 체제를 구동 실행하면 기존 최소 제곱 모델 양태 대비 실효적으로 대단한 향상된 예측 보강 개선(substantial improvement) 양상을 산출해 낼 수 있음을 타전 대변합니다. 그러나 기전의 그림 6.5, 6.8, 6.9 수치 면에서 목격한 능선 회귀(ridge regression) 와 다단 라쏘(lasso) 의 산출 도면을 교차 대조하여 조사해 보면, 지금 이 예시 환경에서는 단면 PCR의 보장 성과 역량이 이 두 가지 위상 축소(shrinkage) 정규화 축적 방법체계 수법보다 못하여 결코 썩 좋지는 못함을 우리는 인지 분별해 지각할 수 있습니다.

The relatively worse performance of PCR in Figure 6.18 is a consequence of the fact that the data were generated in such a way that many principal components are required in order to adequately model the response. In contrast, PCR will tend to do well in cases when the first few principal components are sufficient to capture most of the variation in the predictors as well as the relationship with the response. The left-hand panel of Figure 6.19 illustrates the results from another simulated data set designed to be more favorable to PCR. Here the response was generated in such a way that it depends exclusively on the first five principal components. Now the bias drops to zero rapidly as $M$, the number of principal components used in PCR, increases. The mean squared error displays a clear minimum at $M = 5$. The right-hand panel of Figure 6.19 displays the results on these data using ridge regression and the lasso. All three methods offer a significant improvement over least squares. However, PCR and ridge regression slightly outperform the lasso. 
그림 6.18 구도 하위에서의 상대적으로 불리하고 빈약한 뒤떨어진 참담한 PCR 도출 모델의 성과 지수는, 애당초 반응 계수 체계를 타당히 모델로 포용 적합해 내기 위해 통계 기형상 상당히 막대한 대량의 여러 다수 주성분 컴포넌트 척도가 요구되도록 기저 데이터가 조립 파생 수립되었다는 증명된 사실 체제에 근거 기인한 결과론입니다. 이와는 정반대로 대조하여(In contrast), 초창기 선례인 첫 극소수 파생 단면 주성분만으로도 초기 투입 예측 변수들의 산포된 변동성 분산 파장의 대부분은 물론 척도 타겟 반응 변수와의 연결 상관관계까지 고루 충분히 흡수 포섭 갈무리해 잡아내는 사례 환경 체계에서는 단연코 PCR 기법이 극명히 뛰어난 구동 위력을 뽐내는 경향성이 짙습니다. 단면 그림 6.19 의 측좌 왼쪽 패널 도식 궤도는 의도적으로 PCR 구동 수법에 한층 더 호의적이고 알맞도록 구성 도출 설계된 또 다른 별개의 가상 모의 시뮬레이션 데이터를 통한 구동 결과 수치를 도해하여 보여 줍니다. 여기 도출 배열 데이터 사례 국면에서 이 타겟 반응 편향 변수는 거의 절대적 배타 독점적으로 오로지 처음 5개 도래 추출된 주성분들에만 압도적 전적으로 전면 편승 기인 의존하도록 수립 파생 생성되었습니다. 이제 시스템 PCR 과정에 투입 사용 점유되는 주성분들의 할당 누적 분량인 $M$ 개수가 점차 늘어남에 따라 내재 치우친 오차 편향(bias) 수량은 순식간에 수직 하강하여 영점 원점(zero) 을 향해 급락합니다. 평균 제곱 도출 오차 값은 확고히 $M = 5$ 단위 좌표선에서 선명한 뚜렷한 거대 최저 절저 한계 수치점(clear minimum) 양상을 공시 표출합니다. 우측선 우편 오른쪽 패널 국면의 표식 그림 6.19 국면 도면은 한층 보강된 능선 회귀 체계와 라쏘 조치법을 공통 동일 데이터 전개 상에 병렬 구동 도래하여 측정한 산출 표식 결과의 나열입니다. 비교 참전한 통계 방법 세 기법 모두 단일 최소 제곱 회귀보다 괄목할 엄청난 유의미적 우위 개선점을 증명 도출해 제공합니다. 허나 그럼에도 전체 승부에서 PCR 배열법과 능선 회귀 편향 조율 기법이 단세 라쏘 지표보다도 근소 우위로 보다 살짝 능가하는 양상을 나타냅니다(slightly outperform).

We note that even though PCR provides a simple way to perform regression using $M < p$ predictors, it is _not_ a feature selection method. This is because each of the $M$ principal components used in the regression is a linear combination of all $p$ of the _original_ features. For instance, in (6.19), $Z_1$ was a linear combination of both `pop` and `ad`. Therefore, while PCR often performs quite well in many practical settings, it does not result in the 
우리는 비록 PCR 양식이 단면 투입 예측 인자 범위 $M < p$ 궤도만을 영리히 취합 동원하여 수량 최적화된 회귀를 수행하는 다소 직관 가벼운 매끄러운 단일 명확 방편을 제공하기는 하지만, 이 수법이 절대 고전적 속성 피처 선택 단절 기법(feature selection method) 의 본질 수단은 은연코 _아니다(not)_ 라는 명제 사실에 유념해야 함을 강력히 지적합니다. 그 자명한 까닭은 회귀 모델 진영 속에 투입 편입되어 사용된 각 누적 $M$ 개의 파생 추출 주성분 기저 좌표 체계들 하나하나가 으레 죄다 원천 오리지널(_original_) 속성 다항 변수들 전체 $p$ 개 분량의 무수한 선형 결합(linear combination) 교섭으로 이리 엮이고 저리 파생 성립되었기 때문입니다. 일례를 거론해 보자면 앞전 식 (6.19) 의 위상 구조에서, $Z_1$ 구조물은 초기 투입 지표 `pop` 및 `ad` 쌍 양자 다면 양편을 합친 선형 궤도 혼합 이자 결합체였습니다. 고로 결론적으로, PCR 조치 적용법이 많은 일선 실무 분석 필드 환경 세팅 체계에서 실로 자주 쏠쏠히 제법 우수한 훌륭한 구동 퍼포먼스 효능 결과치 능력을 보일지언정, 이는 절대 

![Figure 6.20](./img/6_20.png)

**FIGURE 6.20.** Left: _PCR standardized coefficient estimates on the_ `Credit` _data set for different values of $M$._ Right: _The ten-fold cross-validation $\text{MSE}$ obtained using PCR, as a function of $M$._ 
**그림 6.20.** 왼쪽 패널: `Credit` _통계 데이터 표본 세트 뭉치에 여러 상이한 축적 스케일 크기의 서로 다른 할당된_ $M$ _편차 값들을 변칙 적용시켜 추출 생성된 PCR 기반 표준화 도출 계수 추정 수치 결괏값들 모음 전개표._ 오른쪽 방향 패널: _변동 할당된 변수 인자 수치_ $M$ _에 편승 대응하는 함수 형태 작용으로 기인해 PCR 체제로 산출 확보 획득된 10-fold 통계 10-겹 교차 검증 도달_ $\text{MSE}$ _잔차 에러 오차 수치._ 

development of a model that relies upon a small set of the original features. In this sense, PCR is more closely related to ridge regression than to the lasso. In fact, one can show that PCR and ridge regression are very closely related. One can even think of ridge regression as a continuous version of PCR![8] 
원본 핵심 특징 피처 초기 자원들의 적은 소규모 선별 세트 부분 집합에만 전면 의존 구동하는 편향된 압축 제한 모형 단일 시스템 체계의 개발이나 진척 단면 수립 도래 단계 결과로는 절대 연결 산출 및 전개 귀결되지 않습니다. 오로지 바로 이러기 같은 고정 의미 관점 진영 체계 하에서 통계학상, PCR 구조 전개 태세 기법 구조는 상대방 위상인 라쏘(lasso) 추정 구조법보다는 역으로 되려 압박 능선 회귀 단면 체제 양상과 훨씬 더 밀접한 구조적 친밀 다변 인척 지표 연관성 상관 동위 결속 관계를 맺고 지닙니다. 사실 파헤쳐 들여 볼수록 단연코 증명 가능하듯, PCR 방식론 기저와 능선 회귀 기저는 수리 모델상 통계적으로 가히 매우 이질 치밀 동질적으로 가깝게도 밀접 친밀 긴밀하게 연결 연관 수립된 기저 친족적 밀월 지배 체계입니다. 우리는 심지어 극단적으로 지표 상 산출된 정규화 위상 양태 거시적 능선 회귀 수리 기법 구조 자체를, 단절 한계 이면 없이 다원 전개되는 PCR 연쇄 체제 기법 시스템의 하나의 "연속적 연속 동조화 전개형 파생 교차 버전(continuous version)"으로 도출 간주하고 단면 생각 체계를 확장해 동일시해 판단할 수도 있습니다![8] 

In PCR, the number of principal components, $M$, is typically chosen by cross-validation. The results of applying PCR to the `Credit` data set are shown in Figure 6.20; the right-hand panel displays the cross-validation errors obtained, as a function of $M$. On these data, the lowest cross-validation error occurs when there are $M = 10$ components; this corresponds to almost no dimension reduction at all, since PCR with $M = 11$ is equivalent to simply performing least squares. 
구동되는 PCR 체계 전제 하에서, 취합 점유할 누적 편입 대상 주성분 요소 할당 개수 척도 수량인 차원 스케일 $M$ 은 전반적 단면상 으레 흔히 보편 전형적으로 널리 쓰이는 표본 교차 검증(cross-validation) 평가 수칙에 의거해 최종 선별 지목 및 적합 선택 결정 조율됩니다. 표본 단면 데이터 축 `Credit` 데이터 기점 세트에 이 단일 PCR 방법 기법 척도를 직접 응용 부착 거점 적용한 결과 산출 전개 양태 양상은 상기 단락 부근의 그림 표면 6.20 위 평면 선상에 공표 표기 표시되어 있습니다; 해당 패널 사진 화면 중 시면을 기점으로 한 가로 우측 오른쪽 영역 동면 패널 화면 단면이, 지정 할당 척도 차원값 $M$ 증폭 수위 인자 변화 대응 함수 전개 곡선으로서 전면 측정 도달로 획득된 잉여 교차 검정 체제 편차 무리 오차 에러 곡선율(cross-validation errors obtained) 분산을 조목조목 전시 전개 도해해 대변 보여줍니다. 위로 관측 분석 대상 적시 도마에 오른 통계 지표 단면 데이터 집단 시스템상에서, 가장 최하 바닥을 친 절저 최저 극저점(lowest) 산포 교차 오차 검증 범위 구간 낙하 에러 발생 현상은 도합 주성분 요소 분량이 $M = 10$ 개 단위(components) 뭉치로 꽉꽉 톱니바퀴 조립 투입 축적 구성 구비 존재할 때야 비로소 실재 포착 단면 도출 귀결 발생합니다; 하지만 이는 사실 기저적으로 치명적인 역설 단서로 그 어느 수준 차원 절묘한 축소 과정(dimension reduction mechanism) 체계 요소도 절대 거의 거시 단면 전혀 털끝조차 사실상 아예 하나도 전무 이뤄 내지 연출해 도출 달성하지 못한 본질 전 상태 원위치 사안 현실에 직접 고스란히 완전 대응 일치 매칭 상응(corresponds) 대변합니다, 왜냐하면 투입 $M = 11$ 척도 범위를 보유 구비한 막장 PCR 구동 방식은 애초 시발점의 무계획 그저 가장 기본 단순하고 평범 순진무구 오리지날 변수 최소 제곱법 단일 기법을 백지상태로 단순 전면 순진하게 수행 파생(simply performing least squares) 치르는 도출 행위 일체 과정과 수학 물리 현상 기하학적으로 일치 매칭 완벽 대변 등치 상응하여 절대(equivalent to) 똑같기 때문입니다.

When performing PCR, we generally recommend _standardizing_ each predictor, using (6.6), prior to generating the principal components. This standardization ensures that all variables are on the same scale. In the absence of standardization, the high-variance variables will tend to play a larger role in the principal components obtained, and the scale on which the variables are measured will ultimately have an effect on the final PCR model. However, if the variables are all measured in the same units (say, kilograms, or inches), then one might choose not to standardize them. 
PCR 궤도 체계를 가동 구동 수리 실행 적합 전개할 때, 우리는 대개 언제나 일선 본체 주성분 무리 인자 요인망들을 초기 시도 단계부터 최초 형성 파생 축적 생성 도출 발탁 추출해 내기 이전에 앞서 미리 사전 수차 조치 선행 기저 기반 작업으로, 수칙 공식 도해 단락 (6.6) 규약을 부착 활용하여 각 투입 진영의 다원 예측 변수 요소 요인 피처 척도들을 일괄 모조리 _표준화(standardization)_ 가공 공정 전처리 정화 조치 제어 취급 통제할 것을 가급적 도의적으로 전면 일반적으로 폭넓게 거시 권장 권고 시사 타전 추천 제안 가이드 척도로 부 여 하 고 제 시 주 목 합 니 다(recommend). 이 필수 거시 표준화 절차 기법은 모든 분별 다채로운 입력 변수 통계 구성 지표 요소 군단들이 결점 무탈히 서로 동일한 동등 격차 등률 측정 척도 스케일 크기 분량 등급(same scale) 상에 올바르게 균형 동일선 안착 위치 진입하도록 보장 확증 공시 확언합니다. 만약 이런 거시 전처리 사전 제어 무장된 표준화 확립 시스템이 전면 공백 부재(absence)하여 누락되는 환경 하 사단에서는, 고분산 즉 높은 오차 진폭의 크고 거대한 자체 엇갈림 오차 파동 분산(high-variance) 스케일을 소유 지니고 우쭐대는 유별 거대 수리 변수 녀석 군단 무리들이 막상 단면 수합 획득된 공고 파생 도출 수립 파생된 핵심 최상 차순위 요인 단일 주성분 지수 뼈대 객체들 안에서 편향 집중되어 월권 부당 상대적으로 극대화 비대 차대 엄청나게 거대한 지배 우위 독점 파장 장악 역할(larger role) 수행 비중 파워 단면 압도를 도래 행사하려는 경향과 편파적 조종 편향 집중 현상 유도를 편향 초래 발휘 꾀하게 될 것이며, 이로 인해 애초 오리지널 투입 통계 변수군들이 원천 자체적으로 측정 계산 단락 적발 파생 수립 체계 산출 관측되었던 그 초기 도량 단위 오만 자체의 스케일 수치 격차가 결국 종국에 가서는 산출 수렴된 최후 막단 종결 산출 마지막 무장 도출 지표 파생된 단층 파이널 분석 최후 PCR 기반 모델 단락의 완성품 품질 전체 전면 형상 도출 수지에까지 전방위 결함 훼손 중대 여파 효과 영향 간섭(an effect on...) 작용 파문을 근본적으로 지대 불가피하게 파생 유발 전면적으로 지독히 연출 행사 미치게 될 것입니다. 그러나 막상 오직 단 조건부 예외(However)적 가정 하나로, 만약 투입 참여 데이터들의 변수 요소 단위 척도 군단들이 몽땅 전부 다 모조리 (가령 다들 킬로그램 단일 중량이라든가 혹은 다 길이 인치 식의) 일체 동일 통일 동등한 단일 도량형 단위 체제 포맷 체계 격식 포맷 수치 단위(say, kilograms, or inches) 기저 단추망 내부 자체에서 일괄 측량 조사 판별 측정 수합 적출 확보 수집 형성 관측되어 마련된 상태 여건 체계라면, 굳이 분석 기법 사용 도출자 입장에서 그런 상태의 오리지날 데이터 무리들에는 모종의 번거로운 가공 규격 압박 무장 적합 환산 억제 차원 인위 지배 관섭 압력 제재 편입 인위 편제 기공 압축 통제 가동을 포기하여 _그것들을 표준화 적제화 표준 동기 조치 처리 적정 무장 적용시키지 않기로(not to standardize them)_ 고의 방조 자유 여지 용인 우회 면책 채택 타결 선택 결단(choose) 결정 내리게 됨을 용인할 수도 다분히 있습니다. 
