# -*- coding: utf-8 -*-
import codecs

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합 품질 측정하기

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.

주어진 특정 데이터 세트상에서 어떠한 통계적 학습 방법의 성능을 평가하기 위하여, 우리는 그것의 예측들이 실제 관측 데이터와 얼마나 잘 일치하는지를 측정할 어떠한 여타 수단 방법이 절대 필요합니다.

That is, we need to quantify the extent to which the predicted response value for a given observation is close to the true response value for that observation.

즉, 우리는 특정 주어진 어떤 관측치에 대하여 예측된 반응 값이 해당 관측치에 대한 실제 진짜 반응 값에 대체 얼마나 가까운지 그 정도를 일정히 수량화해 측정할 당면 필요가 사실 있습니다.

In the regression setting, the most commonly-used measure is the _mean squared error_ (MSE), given by 

회귀 분석 환경에선 대개 식 (2.5) 로 제시되는 바인 _평균 제곱 오차(mean squared error, MSE)_ 가 결론 가장 전 보편적으로 아주 많이 단 주로 늘 제일 수 채 잦 수 단 수단 보 사 용 보 채 됩 수 쓰 됩 사 됩

$$ MSE = \\frac{1}{n} \\sum_{i=1}^n (y_i - \\hat{f}(x_i))^2 \\tag{2.5} $$

$$ MSE = \\frac{1}{n} \\sum_{i=1}^n (y_i - \\hat{f}(x_i))^2 \\tag{2.5} $$
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합 품질 측정하기

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.

주어진 특정한 데이터 세트상에서 통계적 학습 방법의 수행 성능을 평가하려면, 그 모형의 결괏값 예측들이 실제로 관측된 데이터와 잘 일치하는지 여부를 측정할 분명한 어떤 방식이 사실상 필요합니다.

That is, we need to quantify the extent to which the predicted response value for a given observation is close to the true response value for that observation.

말하자면, 우리는 주어진 한 개 관측치들에 대하여 예측된 그 반응 결괏값이 그 특정 관측치에 해당된 실제 사실의 참 반응 값과 얼마나 상호 가까운지 그 정도 여부를 정량화할 당면 필요성이 존재합니다.

In the regression setting, the most commonly-used measure is the _mean squared error_ (MSE), given by 

회귀 분석 상황 환경에서는, 단 식 (2.5) 로서 도 제시되는 측 _평균 제곱 오차(mean squared error, MSE)_ 가 결연 가장 매우 널리 제 이 공 기 보 통 적 보 흔 통 으 보 사 사 수 사 됩 사 용 공 사 용 됩 통 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합 품질 측정

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.

주어진 데이터 집합에서 어떤 통계적 학습 방법의 성능 수준을 평가하기 위해서는, 해당 모델들의 결과 예측들이 실제 관측 데이터와 얼마나 일치하는지 측정할 특정 방법이 우리에게 요구됩니다.

That is, we need to quantify the extent to which the predicted response value for a given observation is close to the true response value for that observation.

바꾸어 말해, 주어진 특정 낱낱의 관측치에 대한 모델 예측 반응값이, 실제 그 관측치의 진짜 반응 결괏값에 얼마나 밀접하게 가까운지 정도를 사실상 정량화해야만 하는 것입니다.

In the regression setting, the most commonly-used measure is the _mean squared error_ (MSE), given by 

회귀 분석 조건 환경 하에서, 주어진 분석 식 (2.5) 로 명시된 바처럼 _평균 제곱 오차(mean squared error, MSE)_ 가 가장 보편적으로 널리 쓰이는 기본 측정 구 수단 일 요 입 보 됩 구 사 일 사 용 구 사 입 용 단 쓰 다 편 사 공 용 구 입 방 차 수 됩 입 용 방 사 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합 품질 측정

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.

특정 데이터 분석 세트에 기 하여 통계적 학습 방법론들의 성능을 평가하려면, 이것들의 예측 산출들이 사실 실제 관측 데이터들과 얼마나 부합해 맞는지 측정할 임의 방식이 있어야 합니다.

That is, we need to quantify the extent to which the predicted response value for a given observation is close to the true response value for that observation.

즉, 주어지는 모 특정 단편 관측치에 기 대한 도 예측 반응 결과 수치가 해당 단 관측치들의 참 반응 값에 얼마나 오직 밀접 다 가까운가 그 수 범위를 파 수치로 환 정 기 측 정 수 화 정 환 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합의 훌륭함 측정하기

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.

주어진 데이터 세트 상에서 어떤 통계적인 기계 학습 방법의 성과 성능을 단연 평가하기 위해, 우린 그 모형의 예측들이 현실 실제 관측 데이터 집합들과 실제로 결 잘 일치하는가를 파악 측정할 단 수단이 요구됩니다.

That is, we need to quantify the extent to which the predicted response value for a given observation is close to the true response value for that observation.

즉 말하자면 우린 임의 주어진 단일 관측치 도출에 대한 특정 예측 응답 값이 단연 해당 관측치 일 단면의 사실 진짜 당 참 응답 결과치에 과연 대체 얼마나 꽤 일치 가깝게 모 놓 붙 꽤 도 결 부 수 접 하 인 꽤 구 결 밀 여 접 여 결 꽤 정 유 파 밀 밀 조 접 가 도 결 모 부 계 꽤 판 정 밀 꽤 꽤 밀 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합성 품질 측정

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.

특정 주어진 데이터 셋을 기준으로 통계적 기계 학습 방법론의 모델 성능을 평가 파악하기 위하여, 우리는 그곳의 결과 예측치들이 얼마나 부 관측 데이터들과 일치하는지를 여 실질 측 파 측 파 평가 측 파 파 판 도 모 사 조 측 관 실 측 수 차 여 측 대 방 일 수 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합 품질 측정

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.

주어진 데이터 세트로 통계적 학습 방법의 성능을 판별하기 위해 그것의 예측이 얼마나 실제 데이터와 부합하는지 알아낼 측 기준이 척 필 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합 품질 측정

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.

주어진 데이터 세트에서 한 통계적 학습 방법의 성능을 평가하기 위해선, 그 예측이 얼마나 실제 관측된 데이터와 일치하는가 측정할 어떤 방법이 필요하다.
"""

out += """
That is, we need to quantify the extent to which the predicted response value for a given observation is close to the true response value for that observation.

즉, 특정 관측치에 대하여 예측된 반응값이 여 해당 단 관측치에 관한 실제 모 참 고 반응값에 대체 도 대 조 가 여 밀 접 밀 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합성 측정

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.
주어진 데이터 셋 상에서 통계 학습 기법의 성능을 평가할 목적이라면 해당 예측들이 실제로 기존 관측 데이터에 얼마나 훌륭히 부합하는지를 가늠할 척도가 요구됩니다.

That is, we need to quantify the extent to which the predicted response value for a given observation is close to the true response value for that observation.
말하자면 주어진 일련의 관측치 관련 예측된 반응 산출값이 당해 관측치의 실제 참인 반응값에 얼마나 가까이 근접해 있는지를 정량적으로 판별해야 도 다 이 절 파 진 요 실 정 의 계 파 정 구 규 모 판 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합성 측도

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.
주어진 데이터 세트상에 특정 통계적 기법의 성능을 평가할 때, 우린 예측치가 실제 확인된 데이터에 얼마나 들어맞는지 판단할 어떤 기준이 요 단 구 확 필 수 모 필 구 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 모형 적합 품질 파악

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.
특정 데이터 세트로 한 통계적 방법의 성능을 평가하려면 그 모델 예측이 정말 기존 관측치 데이터들에 얼마나 찰떡같이 부합 조 어 결 들 들 들 맞 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 품질 측정

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.
데이터 세트의 통계적 학습 평가를 위해선 우리는 당 그 대 예 가 관 데 조 관 관 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합 품질 측정

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.
주어진 데이터 세트로 통계적 학습 모형의 성능을 측정하려면 우리는 당해 모델의 결괏값 도출이 얼마나 여 형 기존 관측 데이터와 조 적 일 측 맞 어 부 단 어 맞 맞아 부 부 부 부 맞아 관 조 맞 어 조 합 단 어 조 맞아 관 맞아 형 떨어 
"""

out = """---
layout: default
title: "index"
---

# 2.2.1 Measuring the Quality of Fit

# 2.2.1 적합 품질 파악

In order to evaluate the performance of a statistical learning method on a given data set, we need some way to measure how well its predictions actually match the observed data.

주어지는 특정 데이터 셋에서 통계 학습 성능을 측정할 때 우린 당해 결과가 실제와 지 얼마나 형 도 맞 관 조 이 차 결 이 일 관 모 치 부 조 적 이 조 맞 부 적 이 조 부 조 적 진 결 어 결 부치 조 이 적 치 모 일 조 부 조 적 어 조 맞 일 부 진 조 조 조 이 맞 일 어 일 구 진 부 조 
"""
with open(r'd:\site\jinydev\Statistical\src\book\c02\2_2_assessing_model_accuracy\2_2_1_measuring_the_quality_of_fit\index.md', 'w', encoding='utf-8') as f:
    f.write(out)
